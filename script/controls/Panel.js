/**	The objects of this class represent the panels for the Network Visualization.
*		The functionality provided for the Panels is as follows:
*		1. The panels can be on the right or the left side of the screen.
*		2. The panels will act as a visual and DOM wrappers for the controls that the user will interact with.
*/
(function(){
	NETWORK.CONTROLS.Panel = function() {
		
	};
	
	NETWORK.CONTROLS.Panel.prototype.attachEvents  = function () {
	};
})(NETWORK.CONTROLS || (NETWORK.CONTROLS = {}));